---
- name: vim conf
  copy:
   src: tools/vimrc
   dest: /home/vagrant/.vimrc
   owner: vagrant
   group: vagrant
- name: make rpm script
  copy:
    src: tools/make_rpm.sh
    dest: /home/vagrant/make_rpm.sh
    owner: vagrant
    owner: vagrant
    mode: "u+x"
